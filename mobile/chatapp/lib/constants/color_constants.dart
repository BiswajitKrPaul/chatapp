import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const Color primary = Color(0xff6200ea);
  static const Color accent = Color(0xff253237);
  static const Color onPrimary = Color(0xffffffff);
}
